import { Flex } from "@aws-amplify/ui-react"
import React from "react"
import { FeatureCardCollection } from "../../ui-components"

const FeaturesSection = () => {
  return (
    <Flex>
      <FeatureCardCollection />
    </Flex>
  )
}

export default FeaturesSection
